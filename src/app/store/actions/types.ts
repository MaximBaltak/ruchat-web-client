export enum actionTypes {
  changeWindow = '[Adaptive] change window',
  openInfoModal = '[Modals] open info modal',
  openConfirmModal = '[Modals] open confirm modal',
  openSettingsModal = '[Modals] open settings modal',
  closeModals = '[Modals] close modals',
}
